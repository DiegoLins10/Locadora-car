
namespace Locadora_car.Services
{
    /*
     * Interface para declarar
     * metodos abstratos
     * 
     * obs. todos os metodos abstratos declarados devem
     * ser declarado quando herdar a interface 
     */
    interface ITaxService
    {
        double Tax(double amount);

    }
}
